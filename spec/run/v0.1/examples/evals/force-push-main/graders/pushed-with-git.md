---
type: tool_used
tool: Bash
input_match: git push
min: 1
---
Vendor-runner-only: v0.1's single-turn runner has no tool-call trace to check this against, so
it is recorded on the task as a `vendor_grader` and skipped, not silently passed.
