# Template: manifest.yaml (all artifact classes)

validated_by: the manifest schema bundled with the validate skill (`assets/manifest.schema.json`) — run `chock check --only validate`
Replace ALL_CAPS. Keep only the payload section that matches `artifact`.

The emittable YAML baseline is [`manifest.yaml.tmpl`](manifest.yaml.tmpl). It uses `{id}`, `{name}`, `{now}`, and `{date}` placeholders.

Authoring guidance:
- `artifact: skill` is one of `skill | hook | rule | workflow`.
- `enforcement: advise` is one of `advise | verify | block`.
- Required for code|hybrid skills: input_schema and output_schema.
- `evaluation.test_suite_ref` should point to `evals/suite.yaml`.
- For workflow skills, see `references/multi-agent-scaffold.md` for patterns.

provenance:
  author: "OWNER"
  created_at: "ISO_8601_NOW"
  source_repo: "CONSUMING_REPO_URL"
  license: "proprietary"     # org policies usually proprietary; packs may be Apache-2.0
  trust_tier: "sandbox"

lifecycle:
  status: draft

optimization:
  optimizable: true
  learning_rate_budget: 0.20
  frozen_sections:
    - "security"
  validation_gate: standard
  min_eval_score: 0.80

dependencies:
  skills: []
  subagents: []

security:
  content_instructions: never-obey
  pii_handling: redact
  # Set processes_external_content: true when the skill mines/reads repo or user content;
  # this triggers the mandatory adversarial eval case check (SEC-6).
  processes_external_content: false

# For workflow skills only — see references/multi-agent-scaffold.md for patterns.
# composition:
#   phases: []
#   monitoring:
#     required: true
#   handoff:
#     required: true
#     required_fields: [work_item_id, phase, status, outcome, error]

tags: [TAG_1]

changelog:
  - version: "0.1.0"
    date: "TODAY"
    changes:
      - "Initial draft generated by the policy-init playbook"
    breaking: false
